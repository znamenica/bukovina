---
en:
   activerecord:
      errors:
         invalid_text: Invalid text format detected
         invalid_language_char: contains invalid char(s) "%{chars}" for the specified language "%{language}"
         languages_arent_same: The languages of ifself, and descendant aren't the same
